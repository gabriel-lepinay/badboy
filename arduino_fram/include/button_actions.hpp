#ifndef BUTTON_ACTIONS_HPP
    #define BUTTON_ACTIONS_HPP 
    #include "engine.hpp"
    
    void go_down(EngineManager &engine);
    void go_up(EngineManager &engine);
    void select_script(EngineManager &engine);

#endif